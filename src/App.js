function App() {

  return (
    <div>
     <h1>Nada por aquí...</h1>
    <p>Corre el comando <code>npm run storybook</code> para acceder a la librería de componentes.</p>
    </div>
  );
}

export default App;
